<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medical Records Management</title>
    <style>
        /* Reset some basic elements for consistency across browsers */
        body,
        h1,
        h2,
        h3,
        p,
        ul,
        li,
        a {
            margin: 0;
            padding: 0;
            list-style: none;
            text-decoration: none;
            font-family: Arial, sans-serif;
        }

        /* Body styling */
        body {
            background-color: #f5f5f5;
            color: #333;
        }

        /* Header styling */
        header {
            background-color: #5cb85c; /* Changed to green */
            color: white;
            padding: 20px 0;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* Container styling */
        .container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
            text-align: center;
        }

        /* Banner image styling */
        .banner {
            width: 10%;
            height: auto;
            border-radius: 10px;
        }

        /* Main heading */
        h1,
        h2 {
            margin: 20px 0;
            color: #5cb85c; /* Changed to green */
        }

        /* Paragraph styling */
        p {
            margin-bottom: 20px;
            line-height: 1.6;
            color: #666;
        }

        /* Button styling */
        button {
            display: inline-block;
            padding: 15px 30px;
            margin-top: 20px;
            background-color: #5cb85c; /* Changed to green */
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #4cae4c; /* Darker green on hover */
        }

        /* Features section styling */
        .features {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            gap: 20px;
            margin-top: 40px;
        }

        /* Individual feature card styling */
        .feature {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: calc(33% - 40px);
            box-sizing: border-box;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .feature:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .feature h3 {
            margin-bottom: 15px;
            color: #5cb85c; /* Changed to green */
        }

        .feature p {
            color: #555;
        }
        
        /* Logout button styling */
        .logout-container {
            text-align: right;
            margin-top: -60px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logout-container">
            <button onclick="window.location.href='/logout'">Logout</button>
        </div>
        <img src="https://i.etsystatic.com/26658960/r/il/e884b9/3814251269/il_fullxfull.3814251269_aqz2.jpg" alt="Medical Records Banner" class="banner">
        <h2>Access and Manage Your Medical Records</h2>
        <p>Keep your personal health data safe, secure, and easily accessible at any time.</p>
        <button onclick="window.location.href='/profile'">PROFILE</button>
        <div class="features">
            <div class="feature">
                <a href="/form" class="button-link">
                    <h3>Add consultations</h3>
                    <p>Add your prescriptions and medical records</p>
                </a>
            </div>
            <div class="feature">
                <a href="/file" class="button-link">
                    <h3>View Records</h3>
                    <p>View your prescriptions, medical reports and other additional documents</p>
                </a>
            </div>
        </div>
    </div>
</body>
</html>
